# Run directories are large (page rasters, crops, the disk cache) and are rebuilt by
# `validation/scripts/run_cisneros.py`. The FIGURES and TABLES the scripts derive from them
# (validation/out/*.png|svg|csv|json|md) are deliverables and ARE committed.
out/run_*/
out/demo_run/
synthetic/corpus/
synthetic/_ingest/
