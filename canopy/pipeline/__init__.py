"""The pipeline: what turns verified values into results.

`resolve` (Task 9) is the only place a study's numbers become an effect size; the orchestrator
(Task 10) drives ingestion, the agents, the verification layer and then this.
"""
