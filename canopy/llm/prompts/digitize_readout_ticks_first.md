
7. Before reading any datum, write down the y-axis tick ladder: every printed tick label and the
   value gap between neighbouring ticks. From that, state how many data units one pixel of the
   image is worth (`pixel_resolution_estimate`), and then read each datum as
   "nearest tick below + a fraction of one tick spacing". Report the ladder in `tick_labels`.
